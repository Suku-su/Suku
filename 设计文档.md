# 笔录/文书模板编辑

在前端操作栏新增编辑，复用新增页面，根据所选id向后端发送请求，将该id的模板内容展示页面上，然后编辑后发往后端,后端处理完成后返回响应码，跳转至列表

后端新增接口：

+ 根据id查询笔录/文书模板

+ 根据id修改笔录/文书模板

前端：

+ 在操作栏下新增编辑
+ 添加对应的编辑函数

## API接口

根据id查询笔录模板

```
GET /record-template/select/{id}
body:
{
"id": 1
}


返回值：
{
"code" : 200,
"msg": "",
"data": {
"id":1,
"court":"1",
"courtName"："主体"，
"reserveType":"应用场景编号"
"reserveTypeName":"应用场景",
"templateName":"模板名称"，
"html":"模板内容"
"signWay":"3",
"remark":"备注信息等等等等等等",
"documentTag":"2,TOP"，
"fileName":"文件名称"，
"fileSuffix":文件后缀，
"weight":1,
"signConfigId":"1"
}
}
```

根据id修改

```
POST /record-template/update/{id}
body：
{
"data": {
"id":1,
"court":"1",
"courtName"："主体"，
"reserveType":"应用场景编号"
"reserveTypeName":"应用场景",
"templateName":"模板名称"，
"html":"模板内容"
"signWay":"3",
"remark":"备注信息等等等等等等",
"documentTag":"2,TOP"，
"fileName":"文件名称"，
"fileSuffix":文件后缀，
"weight":1,
"signConfigId":"1"
}
}

返回值：
{
"code":200,
"msg":"",
"data":""
}
```





---

---





###  文件上传后不保存后续丢失怎么办？

文件名+后缀+tag获取预url

上传完成后保存在minio 

如果再次上传的话，已经有了



是？优化：怎么处理的

n :有一个定时任务来处理7天已经上传的文件

存在哪里了？

minio

+  



+ 保存的模板存在哪里了？

数据库

+ 涉及了那些服务

后台+ recored

 

代码 - > 文档



数据进来的形式

文件民+文件后缀

```
ImportConfirmUploadVO  保存笔录模板
```



返回的数据

```
ResultVO
```

存储的位置

笔录模板落库了

范围

---

---

流程：

新增：



获取文件的预url

```
RecordTemplateController.getRecordPreUrl - > RecordTemplateService.getRecordPreUrl - > recordClient.getRecordPreUrl
文件名+文件后缀+tag            文件名+文件后缀+tag                           文件名+文件后缀+tag

最后返回url
```

笔录服务：

```
getRecordUploadPreUrl（控制层）- >   （服务层）getImportPreUrl - > Tcs(服务层)  - > 返回url
取前50个字符拼接后缀写成文件名          时间+文件名                   判断存储的类型
                                                               Oss一天过期
```

时间+/importRecord+tag

保存到那里了？/不涉及保存，单纯就是获取url





上传笔录模板

后台服务：

```
RecordTemplateController.saveRecord  - > RecordTemplateService.saveRecordTemplate  - > recordClient.saveRecordTemplate
RecordTemplateVO                          RecordTemplateVO                             JSONObject
                                          将ResultVO包装为JSONObject                                             (返回一个ResultVO)
```





笔录模板服务：

```
recode（控制层）接受到了文件 - > （服务层）convertWordAndPdfConfirmToMinioAndDB - > 
拼接并设置文件名                  判断预url是否存在将文件读出来，转为pdf，然后落库，删除缓存以及多余内容
```



不懂这是在干嘛

```java
        recordTemplateVO.setTemplateName(StringEscapeUtils.unescapeHtml4(recordTemplateVO.getTemplateName()));
        recordTemplateVO.setRemark(StringEscapeUtils.unescapeHtml4(recordTemplateVO.getRemark()));
 String referer = req.getHeader("Referer");
```



# 问什么@ResponseBody这个东西使用到了这个位置

```java
 public @ResponseBody
    void saveRecord(@Valid RecordTemplateVO recordTemplateVO, HttpServletResponse resp, HttpServletRequest req) {
        log.info("saveRecord的参数 recordTemplateVO:{}", JSON.toJSONString(recordTemplateVO));
        recordTemplateVO.setTemplateName(StringEscapeUtils.unescapeHtml4(recordTemplateVO.getTemplateName()));
        recordTemplateVO.setRemark(StringEscapeUtils.unescapeHtml4(recordTemplateVO.getRemark()));
        JSONObject resultJson;
        try {
            String referer = req.getHeader("Referer");
            if (referer != null && referer.contains(req.getServerName())) {
                resultJson = recordTemplateService.saveRecordTemplate(recordTemplateVO);
                ResponseUtil.returnJSON(resp, resultJson.toJSONString());
            }
        } catch (Exception e) {
            log.error("saveRecord保存笔录模板异常:", e);
        }
    }
```









---

---

记录：

# redis的库是几



# dsdd端口：





# 后台服务就是配置中心（自己就是）

我需要查看application.yml文件中的Eureka配置部分，了解当前的Eureka客户端和服务端配置情况。
        
            
toolName: view_files
            
status: success
          
            
filePath: d:\workplace\idea\backend-admin\backend-admin\src\main\resources\application.yml
          
我需要继续查看application.yml文件的剩余部分，特别是Eureka相关的配置。
        
            
toolName: view_files
            
status: success
          
            
filePath: d:\workplace\idea\backend-admin\backend-admin\src\main\resources\application.yml
          
根据查看的application.yml配置文件，以下是Eureka的配置方式：

## Eureka配置说明

### 1. 当前配置状态
从配置文件中可以看到，当前应用已经配置了Eureka客户端：

```yaml
eureka:
  instance:
    metadata-map:
      configPath: ${server.servlet.context-path:}
    prefer-ip-address: true
    lease-renewal-interval-in-seconds: 10
    lease-expiration-duration-in-seconds: 30
  client:
    service-url:
      defaultZone: http://172.18.19.102:8761/eureka
    registry-fetch-interval-seconds: 5
```

### 2. 注册到Eureka服务器的配置
要让应用注册到Eureka服务器，需要确保以下配置：

- **eureka.client.register-with-eureka**: true（默认值）
- **eureka.client.service-url.defaultZone**: 设置为Eureka服务器的地址

当前配置已经包含了正确的defaultZone设置。

### 3. 只发现服务而不注册自己的配置
如果只想发现其他服务而不注册自己，需要添加：

```yaml
eureka:
  client:
    register-with-eureka: false  # 不向Eureka注册自己
    fetch-registry: true         # 从Eureka获取注册表（默认true）
    service-url:
      defaultZone: http://172.18.19.102:8761/eureka
```

### 4. 关键配置参数说明

- **register-with-eureka**: true/false - 是否向Eureka注册自己
- **fetch-registry**: true/false - 是否从Eureka获取服务注册表
- **service-url.defaultZone**: Eureka服务器地址
- **registry-fetch-interval-seconds**: 获取注册表的时间间隔（秒）

### 5. 当前配置建议
当前配置已经是完整的Eureka客户端配置，应用会：
1. 向 http://172.18.19.102:8761/eureka 注册自己
2. 每5秒从Eureka服务器获取一次服务注册表
3. 使用IP地址而不是主机名进行注册
4. 租约续约间隔10秒，过期时间30秒

如果需要修改为只发现服务而不注册自己，只需添加 `register-with-eureka: false` 配置即可。
        









# record去发现







# 动态的修改参数

```tex
https://blog.csdn.net/m0_64289188/article/details/140535091?ops_request_misc=%257B%2522request%255Fid%2522%253A%25227de0025a6b453049339d876a7826c234%2522%252C%2522scm%2522%253A%252220140713.130102334.pc%255Fall.%2522%257D&request_id=7de0025a6b453049339d876a7826c234&biz_id=0&utm_medium=distribute.pc_search_result.none-task-blog-2~all~first_rank_ecpm_v1~rank_v31_ecpm-1-140535091-null-null.142^v102^pc_search_result_base8&utm_term=%E6%80%8E%E4%B9%88%E5%9C%A8%E5%90%AF%E5%8A%A8%E7%9A%84%E6%97%B6%E5%80%99%E5%8A%A8%E6%80%81%E7%9A%84%E4%BF%AE%E6%94%B9yml%E7%9A%84%E5%8F%82%E6%95%B0%E8%AE%BE%E7%BD%AE&spm=1018.2226.3001.4187
```

动态的修改参数，而不是修改yml文件

应用配置命令行传参，启动jar包通过命令行传参。

![image-20250902151821632](C:\Users\Administrator\Desktop\ku\项目任务\任务1\assets\image-20250902151821632.png)

+ 打包成jar包，在jar包外使用复制一份application.yml文件，jar是默认当这份文件不存在的时候才会使用内部的。



# 原文件名1

# pdftcs与上传文件的关系

# weight赋值的位置

# sign_config_id

# 文件上传

+ # 直接上传到服务器

+ # 服务器到储存地址、给客户端

+ 流程+自己写demo

   + 直接给服务器的
   + 服务器gei



# springboot服务地址前缀



api/

接口规范：

+ 加密：

  客户端调用的接口、需要对互联网开放，并使用加密方式进行改造
  对互联网开放的接口又分为：
  +  对客户端开放的接口
  +  给第三方带调用的接口

+ 不加密：

​       服务间相互调用的接口，且不会对互联网访问





+ 对客户端开放的接口

  + 在controller包下创建outer包，url统一增加前缀，路径变更为/ots/api/v1/reserve -> /ots/outer/api/v1/reserve
  + 在网关增加对应的配置
  + 接口的类型转为post，数据放在data中
  + 使用aes解密
  + 客户端访问新接口，解密通过后访问原来的接口
  + 返回值不变

+  服务间相互调用的接口

  + 在controller包下创建outer包，url统一增加前缀，路径变更为/ots/api/v1/reserve -> /ots/inner/api/v1/reserve
  + 参数以及返回值都不需要修改
  + 对外屏蔽，直接调用原来的老接口
  + 在网关配置中修改，只有将 /ots/inner/** 路由至不存在的服务:这样就实现了有人通过网关外部访问的时候，也无法通过此类接口路由。
  + 服务器修改为新的地址
  
  
  
+ 对第三方的接口
  
  + 不确定怎么使用，严格限制权限
  + 按照AES加密配置密钥，2400:{secret:123abc, isSingle:true}，其中2400为法院编号，secret:123abc为密钥, isSingle:true表示一家法院，如果为false则代表下辖（默认true）；
  
  
  
  接口规范：
  
  ```json
  body:
  {
      "data":"" //原始json字符串aes加密串，加密前json如下：
  }
  //加密前json
  {
      "tag":"chat",
      "reserveId":"",
      "fileName":"",
      "fileSuffix":""
  }
  ```
  
  + 在controller下创建对应的包，报下的代码与报名对应，
  
  - 对外的接口
  
    + 客户端需要将请求的参数加密，加密后需要请求的接口也需要来修改为对用的post类型来解密验证，接受参数。
  
    + 对第三方的接口，需要更加严格的限制，使用2400：{sercret:123abc,isDSingle:true} 添加到对应的包以及修改为post，在数据处理逻辑中还要增加数据范围的校验，不能超出court参数所属的法院范围；
  
    + 特殊情况，接口调用方位全国统建系统，则不需要校验数据范围，仅解密即可
  
    + 根据情况具体的分析，如果涉及具体的法院/不是真正的法院等，可以只解密
  
+ 接口梳理范围
  
  按分类梳理范围，各自对应调整
  
  ---
  
  PC客户端（C++、C#）`done`
  
  安卓 `done`
  
  小程序 `done`
  
  ios（参考安卓） `done`
  
  后台管理（页面调用的接口）
  
  其他
  
  - web客户端
  - 富文本笔录（cloud-web-record） `done`
  - 成效平台（application_platform_web）
  - 视频下载工具（video-backup）
  - 二维码证据上传web项目（cloud-web） `done`
  - 调解RPA（独立程序、浏览器插件） `done`
  - 数据摆渡外网(data-ferry-out)

### 4.2 服务间相互调用的接口

梳理所有服务（共17个）的FeignClient、HttpClient、RestTemplete调用的接口：

- backend
- ots
- record
- testimony
- sms
- dtsout
- saas-callback
- nemo
- cim
- recognition
- csc
- gateway
- mina-audio
- audio-out
- ftp2http
- meeting-service
- dsdd

### 4.3 对第三方开放的接口

- 参考历史对外接口文档

## 5.接口详情

详细的接口列表及修改描述：参考单独接口详情文档
